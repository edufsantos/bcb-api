export type Envs = {
  NODE_ENV: string;
  BASE_URL: string;
  PORT: string;
  CLIENT_URL: string;
  JWT_SECRET: string;
  JWT_EXPIRES_IN: string;
  SMTP_HOST: string;
  SMTP_USER: string;
  SMTP_PASS: string;
  SMTP_DEFAULT_FROM: string;
};
